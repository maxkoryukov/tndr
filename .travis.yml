sudo: false

language: node_js

node_js:
  - stable

cache:
  directories:
    - node_modules

before_script:
  - npm install -g gulp

script:
  - gulp

#after_script:
#  - if [ $SENDCOVERAGE ]; then ./tools/coverage-calc.sh; ./tools/coverage-send.sh && rm -rf ./coverage; fi
